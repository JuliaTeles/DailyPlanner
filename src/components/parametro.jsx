import React from 'react'

function Parametro (param){
    return(
        <div>
            <h1>{param.titulo}</h1>
            <p>{param.subtitulo}</p>
        </div>
    )
}

export default Parametro